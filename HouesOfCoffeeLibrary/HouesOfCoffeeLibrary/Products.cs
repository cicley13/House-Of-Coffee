namespace HouesOfCoffeeLibrary
{
    public class Products
    {
        public enum Coffeesize { small=1, medium, large }
        public enum SandwichTypes { EggSandwich=1, ChickenBiscuit }
        public enum MenuOptions { Coffee = 1, Sandwich, DailyTotal, Employees }
    }
}
